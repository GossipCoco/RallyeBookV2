import { createStore } from 'vuex'
import { auth } from './auth.module'
export default createStore({
  auth,
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
      auth
    }
  })
